# Anthropic / OpenAI keys used by the AI simulation + TTS/STT engine
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# NextAuth
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# Legacy env-var-provisioned users (see SECURITY.md) — JSON array
AUTH_USERS=

# Upstash Redis (results storage)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Supabase (scenario content) — safe to leave as the defaults baked into
# lib/supabase.js; only set these if the project or anon key changes.
SUPABASE_URL=https://isgdzeyftlpvvqzjuzqt.supabase.co
SUPABASE_ANON_KEY=
